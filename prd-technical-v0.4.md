# 结伴蛙 (Join Frog) · 技术 PRD v0.5
**平台类型**：C2C 线下陪伴撮合 · 双 App 架构
**文档日期**：2026-03-12（最新）
**数据来源**：产品讨论 + AI 协作规则文档

---

## 变更日志

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v0.5 | 2026-03-12 | 首页→等级+收藏页；抽卡UI重设计(Join Frog风格/真实照片)；底部导航橙色✨中心键；收藏系统；评价日/周期规则；手续费规则；定价范围；升级条件邀请机制；全款支付流程；新增5个交互页面 |
| v0.4 | 2026-03-06 | 用户等级(L0-L3)；成长值；陪逛员5等级；保证金；取消/迟到规则；聊天敏感词；私下交易处罚 |
| v0.3 | 2026-03-05 | 初始PRD结构；抽卡系统；订单状态机；陪逛员入驻流程 |

---

## 一、系统角色

| 角色 | 说明 |
|------|------|
| User（用户） | 下单方，使用结伴蛙 App |
| Companion（陪逛员） | 服务提供者，使用结伴蛙审核 App |
| Admin（平台管理员） | 后台运营，处理审核/违规/纠纷 |

---

## 二、用户系统

### 2.1 用户等级

| 等级 | 名称 | 说明 |
|------|------|------|
| L3 | 核心会员 | 最高级，高活跃忠实用户 |
| L2 | 活跃会员 | 中级，完成多次订单 |
| L1 | 基础会员 | 默认注册等级 |
| L0 | 风险用户 | 因行为扣分降级，状态对陪逛员可见，双方均有选择权 |

> L0 用户**可以交易**，但其风险状态在"我的"页面和陪逛员端均可见，陪逛员有权拒绝 L0 用户的订单。

### 2.2 用户成长值系统

```
初始值：growth_point = 0

扣分行为：
  提前 24 小时以上取消订单：-2
  24 小时内取消订单：        -4

降级规则：
  if growth_point <= -6:
    user_level = L0

L0 恢复机制：
  完成 1 单 + 评价 → 恢复至 L1
```

### 2.3 用户等级升级条件

```
L1 → L2：
  完成 1 单 + 有效评价

L2 → L3：
  在两个评价周期内完成 6 单

评价周期定义：
  周期 A：每月 5 日 → 25 日
  周期 B：每月 25 日 → 次月 5 日
  两个周期 = 一个统计周期（约 60 天）
```

### 2.4 用户每日抽卡额度

| 等级 | 每日可刷蛙蛙数 | 备注 |
|------|--------------|------|
| L3 | 无限制 | 核心会员特权 |
| L2 | 50 张 | — |
| L1 | 20 张 | 默认等级 |
| L0 | 5 张 | 风险用户限流，仍可浏览和交易 |
| 开发者模式 | 无限制 | 仅测试用 |

### 2.5 用户等级权益

| 权益项目 | L3 | L2 | L1 | L0 |
|---------|----|----|----|----|
| 每日卡刷新 | 无限制 | 50张 | 20张 | 5张 |
| 匹配高等级蛙蛙 | 优先最高等级 | 高概率 | 较高概率 | ✗ |
| 金牌客服 | 专属客服 | ✓ | ✗ | ✗ |
| 共创社区管理员 | ✓ | ✗ | ✗ | ✗ |

---

## 三、用户手续费规则

```
平台基础手续费率：platform_fee_rate = 10%

等级折扣：
  L3：6.5 折 → 实际费率 = 10% × 65% = 6.5%
  L2：8.5 折 → 实际费率 = 10% × 85% = 8.5%
  L1：无折扣 → 实际费率 = 10%
  L0：无折扣 → 实际费率 = 10%（可交易但无优惠）

计算公式：
  fee = order_amount × platform_fee_rate × discount
```

---

## 四、陪逛员系统

### 4.1 陪逛员等级

| 等级 | 英文 | 说明 |
|------|------|------|
| 未定级 | Unrated | 资料未审核通过 |
| 黑铁 | BlackIron | 审核通过，初始等级 |
| 白银 | Silver | 完成基础订单 |
| 黄金 | Gold | 高完成率 + 复购要求 |
| 王者 | King | 各省 Top 贡献，每 60 天评定 |

### 4.2 陪逛员等级升级条件

```
Unrated → BlackIron：
  资料审核通过（5步骤）

BlackIron → Silver：
  完成 1 单 + 收到用户评价

Silver → Gold：
  完成订单 ≥ 15 单
  + 好评率 ≥ 80%
  + 满足复购条件：
      方案A：2个不同用户，各复购 ≥2次
      OR
      方案B：1个用户复购 ≥3次
  + 额外条件：需 1名担保用户
      担保用户必须满足：完成 ≥2单 且 注册 ≥30天
  + 所有订单必须为不同日期

Gold → King（王者）：
  当前为 Gold 等级
  + 系统抽成贡献排名前 5%（省级）

王者评定周期：
  4个评价周期 ≈ 60 天
```

### 4.3 陪逛员等级权益

| 权益 | 王者 | 黄金 | 白银 | 黑铁 | 未定级 |
|------|------|------|------|------|--------|
| 创始人计划 | ✓ | ✗ | ✗ | ✗ | ✗ |
| 议价范围（相对标准价） | 无限制 | ±75% | ±35% | ±15% | ✗ |
| 客户通话权限 | ✓ | ✓ | ✓ | ✗ | ✗ |
| 社区共建参与 | ✓ | ✓ | ✗ | ✗ | ✗ |
| 极速客服 | 专属 | ✓ | ✗ | ✗ | ✗ |

### 4.4 陪逛员保证金

```
大学生认证用户：deposit = ¥200
非学生：         deposit = ¥500
```

### 4.5 陪逛员接单规则

```
每单最多服务人数：max_customers_per_order = 2
每日最多接单数：  max_orders_per_day = 2

包天订单规则：
  if order_type == full_day:
    disable_other_orders_today = true
  提示文案："该蛙蛙当天已存在包天条件"
```

---

## 五、陪逛员入驻流程（5步骤）

```
Step 1：身份证上传（正反面）
Step 2：人脸识别（与身份证比对）
Step 3：协议签订（电子签名）
Step 4：视频承诺录制（内容规范待定）
Step 5：平台审核（1-3 工作日）

审核通过 → 状态变为 BlackIron，可设置名片/定价
审核拒绝 → 显示原因，支持补充材料重新提交
```

---

## 六、订单系统

### 6.1 服务时间与定价

```
可服务时段：06:00 – 21:30

最低订单时长：2 小时

定价计算：
  标准：pay = hour_price × hours
  6小时优惠：pay = hour_price × 5（相当于6小时收5小时费）
  包天：pay = hour_price × 8
```

### 6.2 支付规则

```
支持方式：微信 / 支付宝 / 云闪付 / 银行卡

资金流转：
  用户支付 → 进入平台托管账户
  服务完成后 24 小时 → 结算至陪逛员账户（扣除抽佣）
```

### 6.3 平台抽佣（陪逛员侧）

```
抽佣比例按陪逛员等级：
  黑铁：20%
  白银：18%
  黄金：15%
  王者：10%
```

### 6.4 订单状态机

```
用户下单 + 付定金
    ↓
  待确认（陪逛员 24h 内响应）
    ↓ 接单          ↓ 拒单 / 超时未响应
  进行中           已取消（定金退回）
    ↓ 服务完成
  待评价（双方互评）
    ↓ 评价完成
  已完成（24h 后结算）

异常路径：
  进行中 → 陪逛员爽约 → 赔偿规则见 §7
  进行中 → 用户触发安全规则 → 陪逛员可立即结束
  进行中 → 私下交易检测 → 双方处罚见 §10
```

---

## 七、取消与迟到规则

### 7.1 用户取消

| 时间节点 | 退款金额 | 成长值变化 |
|---------|---------|-----------|
| 提前 24h 以上 | order_amount - 2%（手续费） | -2 |
| 24h 内取消 | order_amount - hour_price × 30%，平台抽成 7% | -4 |
| 新用户保护（首单 24h 内取消） | 100% 退款 | -4 |

### 7.2 陪逛员迟到

```
判定条件：用户已 GPS 定位签到 + 系统拍照确认

处罚：
  赔偿用户 1 小时费用
  额外扣除 ¥7 手续费
```

### 7.3 用户迟到

```
0–15 分钟：系统提醒陪逛员等待
15–30 分钟：用户需支付 ¥2/分钟，平台额外收 ¥2/5分钟
≥30 分钟：
  陪逛员可申请取消订单
  平台处理：冻结用户 1 小时费用
  平台抽成：7%
```

---

## 八、签到系统

```
签到要求：
  GPS 定位（双方）+ 系统拍照

禁止：
  相册上传替代实时拍照

用途：
  作为迟到/爽约的法律依据
  作为服务开始的时间戳
```

---

## 九、聊天系统

```
聊天模式：订单内聊天（不开放自由聊天）

触发流程：
  抽卡 → 点 GO! → 填写预约信息 → 订单确认 → 开启聊天

敏感词检测（触发强制审核）：
  微信 / vx / wx / qq / wechat / 加我 / 电话 / 联系方式
  违规处理：消息被拦截 + 强制人工审核

设计原则：所有沟通留在平台内，防止私下交易
```

---

## 十、行为监管与处罚

### 10.1 陪逛员禁止行为

```
禁止：
  强制消费（要求用户购买指定商品）
  强制路线（偏离约定行程）
  利益绑定商家（暗中收取商家返利）

处罚：
  扣除全部保证金
  赔付用户损失
```

### 10.2 安全规则（用户侵害陪逛员）

```
触发条件（用户出现以下行为）：
  骚扰 / 性暗示 / 肢体接触 / 威胁

陪逛员权利：
  立即结束订单（无需等待平台确认）

平台处置：
  首次：警告
  情节严重或再犯：封号
```

### 10.3 私下交易处罚

```
用户主动发起私下交易：
  降级至 L0
  平台按订单金额收取抽佣
  补偿陪逛员损失

陪逛员主动发起私下交易：
  扣除全部保证金
  账号封禁
```

---

## 十一、数据模型

### User

```
User {
  uid: string               // U-YYYYMMDD-XXXX
  nickname: string
  phone: string
  level: enum               // L0 | L1 | L2 | L3
  growth_point: int         // 初始 0
  daily_card_quota: int     // L0=5, L1=20, L2=50, L3=∞
  is_risk_user: bool        // level==L0 时为 true，对陪逛员可见
  trip_count: int
  platform_fee_discount: float  // L3=0.65, L2=0.85, L1=1.0, L0=1.0
  created_at: timestamp
}
```

### Companion

```
Companion {
  cid: string
  tier: enum                // Unrated | BlackIron | Silver | Gold | King
  deposit_paid: int         // 200（学生）或 500（非学生）
  commission_rate: float    // King=10%, Gold=15%, Silver=18%, BlackIron=20%
  price_per_hour: int
  price_range: float        // 议价范围：King=无限, Gold=±75%, Silver=±35%, BlackIron=±15%
  max_daily_orders: int     // = 2
  max_customers_per_order: int  // = 2
  good_review_rate: float
  completed_orders: int
  contribution_rank_pct: float  // 省级贡献排名，King 需 ≤5%
}
```

### Order

```
Order {
  oid: string
  user_id: string
  companion_id: string
  status: enum              // 待确认 | 进行中 | 待评价 | 已完成 | 已取消
  order_type: enum          // standard | full_day
  start_time: timestamp
  duration_hours: float
  location: string
  scene: string
  total_price: int
  deposit_paid: int
  platform_fee: int
  user_rating: float
  companion_rating: float
  created_at: timestamp
}
```

---

## 十二、待定事项

| 功能 | 状态 | 备注 |
|------|------|------|
| L0 用户是否有额外惩罚费率 | 待定 | 目前同 L1 = 10% |
| 视频承诺录制内容规范 | 待定 | — |
| 担保用户机制的前端入口 | 未设计 | Silver→Gold 升级需要 |
| 王者抽成贡献排名算法 | 未定义 | 按交易额 or 订单数 |
| 冷启动方案 | 未完成 | — |
| 功能优先级 P0/P1/P2 | 未划分 | — |
| 数据埋点 | 未定义 | — |
| 复购条件的前端可视化 | 未设计 | — |
| 陪逛员违约/爽约具体扣款 | 待补充 | — |

---

*v0.4 · 2026-03-06 · 可直接复制给 AI 或同事使用*
